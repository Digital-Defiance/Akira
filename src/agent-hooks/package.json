{
  "name": "@akira/agent-hooks",
  "version": "0.1.0",
  "description": "Agent hooks system for Akira - automated agent actions triggered by IDE events",
  "main": "./dist/extension.js",
  "types": "./dist/extension.d.ts",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "test": "mocha --require ts-node/register 'tests/**/*.test.ts'",
    "lint": "eslint src --ext ts"
  },
  "dependencies": {
    "ajv": "^8.12.0",
    "ajv-formats": "^2.1.1"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/vscode": "^1.85.0",
    "@types/mocha": "^10.0.6",
    "typescript": "^5.3.2",
    "mocha": "^10.2.0",
    "ts-node": "^10.9.2",
    "@vscode/test-electron": "^2.3.8"
  },
  "engines": {
    "vscode": "^1.85.0"
  }
}
